<div class="container">
<h2><%= @list.name %></h2>
<%= form_for [@list, @book] do |f| %>
<% if @book.errors.any? %>
	    <div id="errors"
	      <h2><%= pluralize(@book.errors.count, "error") %> prevented this book from being saved</h2>
	      <ul>
		    <% @book.errors.full_messages.each do |msg| %>
		      <li><%= msg %></li>
		      <% end %>
		      <% end %>
	     </ul>
	    </div>

 <h2>Add a book</h2>
   <p>
		<%= f.label :name %><br>
		<%= f.text_field :name %>
	<p>
	<p>
		<%= f.label :author %><br>
		<%= f.text_field :author %>
	<p>
	<p>
		<%= f.label :pages %><br>
		<%= f.text_field :pages %>
	<p>
	<p>
		<%=	f.submit "Add a book" %>
	</p>
</div>


<% end %>